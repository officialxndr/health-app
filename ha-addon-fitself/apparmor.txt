#include <tunables/global>

profile fitself flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  network,
  capability,
  file,
  signal (send) set=(kill,term,int,hup,cont),

  # Node.js runtime
  /usr/bin/node ix,
  /usr/bin/npm ix,

  # PostgreSQL
  /usr/bin/pg_ctl ix,
  /usr/bin/postgres ix,
  /usr/bin/initdb ix,
  /usr/bin/psql ix,
  /data/postgres/** rwkl,
  /data/postgres.log rwkl,

  # nginx
  /usr/sbin/nginx ix,
  /etc/nginx/** r,
  /var/log/nginx/** w,
  /run/nginx.pid rwk,

  # Application
  /app/** r,
  /app/apps/api/node_modules/** ix,
  /tmp/** rw,
  /data/** rwkl,

  s6-overlay,
}
